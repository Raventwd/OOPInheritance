public class Bus extends Transport{
    public Bus(String brand, String model, String color, int maxSpeed, int year, String country){
        super(brand, model, year, country, color, maxSpeed);
    }
}
